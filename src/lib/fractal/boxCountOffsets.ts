/** Box counting with multiple grid-origin offsets to reduce alignment bias. */
// TODO: Phase 1
export function boxCountWithOffsets(_binary: Uint8Array, _width: number, _height: number, _boxSize: number, _offsets: number[]): { mean: number; min: number; max: number; std: number } { return { mean: 0, min: 0, max: 0, std: 0 }; }
