/** Auto-select valid box sizes (powers of 2) based on image dimensions. */
// TODO: Phase 1
export function autoSelectBoxSizes(_width: number, _height: number): number[] { return [4, 8, 16, 32, 64, 128, 256]; }
