/** Box-counting algorithm — count occupied boxes at a given box size. */
// TODO: Phase 1
export function boxCount(_binary: Uint8Array, _width: number, _height: number, _boxSize: number): number { return 0; }
