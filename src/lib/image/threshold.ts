/** Apply threshold to grayscale array, producing a binary Uint8Array. */
// TODO: Phase 1
export function applyThreshold(_grayscale: Uint8Array, _threshold: number, _invert?: boolean): Uint8Array { return new Uint8Array(); }
