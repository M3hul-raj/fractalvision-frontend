/** Morphological operations — erosion, dilation, opening, closing. */
// TODO: Phase 3
export function erode(_binary: Uint8Array, _width: number, _height: number): Uint8Array { return new Uint8Array(); }
export function dilate(_binary: Uint8Array, _width: number, _height: number): Uint8Array { return new Uint8Array(); }
