/** Crop tool — extract a rectangular region from an image. */
// TODO: Phase 3
export function cropImage(_imageData: ImageData, _x: number, _y: number, _w: number, _h: number): ImageData | null { return null; }
