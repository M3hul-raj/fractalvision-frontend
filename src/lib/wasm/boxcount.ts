/** TypeScript bindings for the WASM box-counting module. */
// TODO: Phase 10
export async function wasmBoxCount(_binary: Uint8Array, _width: number, _height: number, _boxSize: number): Promise<number> { return 0; }
